---
title: SDKs
description: SDKs oficiais do MillionSend para nove linguagens, publicados sob MIT.
---

Cada SDK espelha o formato do SDK oficial correspondente do Resend, então
migrar é quase um localizar-e-substituir: troque o import/classe e aponte a
URL base para o MillionSend. Todos os SDKs são licenciados sob MIT e vivem em
repositórios próprios na
[organização MillionSend no GitHub](https://github.com/MillionSend).

Como a API é compatível na comunicação com a do Resend, os SDKs oficiais do
Resend também funcionam — aponte-os para o MillionSend pela opção de URL base
(ex.: `RESEND_BASE_URL`).

## URL base

<DeploymentTabs>
<DeploymentTab deployment="cloud">

`https://api.millionsend.com` — o que todos os exemplos desta página usam.

</DeploymentTab>
<DeploymentTab deployment="self-hosted">

A origem da API da sua instância (ex.: `https://api.acme.dev`, ou
`http://localhost:3001` localmente). Todo SDK a recebe pela mesma opção em
que os exemplos passam a URL da Nuvem — `baseUrl` / `base_url` / `BaseURL`
conforme a convenção da linguagem — e o SDK de Node também lê a variável de
ambiente `MILLIONSEND_BASE_URL`. Veja
[Auto-hospedagem](/pt-BR/self-hosting) para implantar a instância em si.

</DeploymentTab>
</DeploymentTabs>

## Node.js / TypeScript

[`millionsend` no npm](https://www.npmjs.com/package/millionsend) — Node 18+.

```sh
npm install millionsend
```

```ts
import { MillionSend } from "millionsend";

const ms = new MillionSend("ms_123", { baseUrl: "https://api.millionsend.com" });

const { data, error } = await ms.emails.send({
  from: "Acme <onboarding@acme.dev>",
  to: "delivered@example.com",
  subject: "Hello from MillionSend",
  html: "<strong>It works!</strong>",
});
```

## Python

[`millionsend` no PyPI](https://pypi.org/project/millionsend/) — Python 3.9+.

```sh
pip install millionsend
```

```python
import millionsend

millionsend.api_key = "ms_123"
millionsend.base_url = "https://api.millionsend.com"

email = millionsend.Emails.send({
    "from": "Acme <onboarding@acme.dev>",
    "to": "delivered@example.com",
    "subject": "Hello from MillionSend",
    "html": "<strong>It works!</strong>",
})
```

## Go

[`millionsend-go`](https://github.com/MillionSend/millionsend-go) — Go 1.21+.

```sh
go get github.com/MillionSend/millionsend-go
```

```go
client := millionsend.NewClient("ms_123")
client.BaseURL = "https://api.millionsend.com"

sent, err := client.Emails.Send(&millionsend.SendEmailRequest{
    From:    "Acme <onboarding@acme.dev>",
    To:      []string{"delivered@example.com"},
    Subject: "Hello from MillionSend",
    Html:    "<strong>It works!</strong>",
})
```

## Ruby

[`millionsend` no RubyGems](https://rubygems.org/gems/millionsend) — Ruby 3.0+.

```sh
gem install millionsend
```

```ruby
require "millionsend"

Millionsend.api_key  = "ms_123"
Millionsend.base_url = "https://api.millionsend.com"

email = Millionsend::Emails.send(
  from: "Acme <onboarding@acme.dev>",
  to: "delivered@example.com",
  subject: "Hello from MillionSend",
  html: "<strong>It works!</strong>"
)
```

## PHP

[`millionsend/millionsend-php` no Packagist](https://packagist.org/packages/millionsend/millionsend-php) — PHP 8.1+.

```sh
composer require millionsend/millionsend-php
```

```php
use MillionSend\MillionSend;

$ms = MillionSend::client('ms_123', 'https://api.millionsend.com');

$email = $ms->emails->send([
    'from' => 'Acme <onboarding@acme.dev>',
    'to' => 'delivered@example.com',
    'subject' => 'Hello from MillionSend',
    'html' => '<strong>It works!</strong>',
]);
```

## Rust

[`millionsend` no crates.io](https://crates.io/crates/millionsend) — async
(`tokio` + `reqwest`).

```toml
[dependencies]
millionsend = "0.2"
tokio = { version = "1", features = ["macros", "rt-multi-thread"] }
```

```rust
use millionsend::{MillionSend, SendEmailOptions};

let ms = MillionSend::with_base_url("ms_123", "https://api.millionsend.com");

let sent = ms.emails.send(&SendEmailOptions {
    from: "Acme <onboarding@acme.dev>".into(),
    to: "delivered@example.com".into(),
    subject: "Hello from MillionSend".into(),
    html: Some("<strong>It works!</strong>".into()),
    ..Default::default()
}).await?;
```

## Java

[`com.millionsend:millionsend-java` no Maven Central](https://central.sonatype.com/artifact/com.millionsend/millionsend-java) — Java 11+.

```xml
<dependency>
  <groupId>com.millionsend</groupId>
  <artifactId>millionsend-java</artifactId>
  <version>0.2.0</version>
</dependency>
```

```java
MillionSend ms = new MillionSend("ms_123", "https://api.millionsend.com");

CreateEmailResponse sent = ms.emails().send(
    CreateEmailOptions.builder()
        .from("Acme <onboarding@acme.dev>")
        .to("delivered@example.com")
        .subject("Hello from MillionSend")
        .html("<strong>It works!</strong>")
        .build());
```

## .NET

[`MillionSend` no NuGet](https://www.nuget.org/packages/MillionSend) — alvo net8.0.

```sh
dotnet add package MillionSend
```

```csharp
using MillionSend;

var client = new MillionSendClient("ms_123", "https://api.millionsend.com");

var res = await client.EmailSendAsync(new EmailMessage
{
    From = "Acme <onboarding@acme.dev>",
    To = "delivered@example.com",
    Subject = "Hello from MillionSend",
    Html = "<strong>It works!</strong>",
});
```

## Elixir

[`millionsend` no Hex](https://hex.pm/packages/millionsend) — Elixir 1.15+.

```elixir
# mix.exs
def deps do
  [{:millionsend, "~> 0.2"}]
end
```

```elixir
config :millionsend, MillionSend.Client,
  api_key: System.get_env("MILLIONSEND_API_KEY"),
  base_url: "https://api.millionsend.com"

{:ok, email} =
  MillionSend.Emails.send(%{
    from: "Acme <onboarding@acme.dev>",
    to: "delivered@example.com",
    subject: "Hello from MillionSend",
    html: "<strong>It works!</strong>"
  })
```

import { OverviewView } from "@/components/console/overview/overview-view";

/** The console's front page: KPI cards, stat tiles, region cards, sent per day and health. */
export default function ConsoleOverviewPage() {
  return <OverviewView />;
}

# Supply-chain: only install package versions published at least 24h ago
# (absorbs the Shai-Hulud-style zero-day-malware window; pnpm 11 default made explicit).
minimum-release-age=1440

# Lifecycle scripts are opt-in per package via onlyBuiltDependencies (pnpm 11 default).
strict-peer-dependencies=false
